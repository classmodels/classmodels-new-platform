# Optie A — alleen op Node-instance "classmodels-web"
# Websites: www + api → backend = classmodels-web
# Zie scripts/combell-optie-a.txt voor uitleg en wat je aan Combell vraagt

COMBELL_HOST_ROUTER=1
# Cloudflare R2 (grote foto's/ZIP's/films — verplicht voor mediatheek op schaal):
MEDIA_BACKEND=r2
R2_BUCKET=classmodels-media
R2_ACCOUNT_ID=VERVANG_MET_CLOUDFLARE_ACCOUNT_ID
R2_ENDPOINT=https://VERVANG_ACCOUNT_ID.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=VERVANG_ACCESS_KEY
R2_SECRET_ACCESS_KEY=VERVANG_SECRET_KEY
# Geen ".", placeholder of leeg — anders start geen dual-proxy (geen Nest, geen /media/public).
DB_URL=VERVANG_VOLLEDIGE_MYSQL_URL_VAN_COMBELL
JWT_SECRET=VERVANG_LANG_WILLEKEURIG_GEHEIM
CORS_ORIGIN=https://www.class-models.be,https://class-models.be
API_PUBLIC_URL=https://api.class-models.be
NEXT_PUBLIC_API_URL=https://api.class-models.be
# Zelfde waarde bij **web build** (Combell pipeline vóór `npm run build`): anders kunnen <img>-URL’s fout blijven.
NEXT_PUBLIC_APP_URL=https://www.class-models.be
WEB_PUBLIC_URL=https://www.class-models.be
# Tijdelijk: modellen kunnen inloggen met import-wachtwoord; hash wordt bij eerste login gezet
LEGACY_IMPORT_PASSWORD=classmodels2026!
WEB_APP_URL=https://www.class-models.be
NODE_ENV=production
# Mediatheek op 100 GB hosting-quota (niet alleen kleine /app/shared-container):
# CM_COMBELL_DATA_UPLOADS=/data/sites/web/class-modelsbe/www/cm-media/uploads
# dual-proxy kiest dit automatisch als de map schrijfbaar is vanuit Node.
# Zonder dual-proxy: zet zelf absoluut pad. Gebruik geen relatief "uploads" in apps/api/.env
# (overschrijft de proxy en schrijft in de release-map → alles weg na deploy).
# MEDIA_ROOT=/home/JOUWUSER/www/cm-media/uploads
# MEDIA_SYNC_SOURCE=/pad/naar/oude-uploads-bron
# Grote ZIP in mediatheek (admin): max 6 GB; één POST (geen chunked upload)
# MEDIA_ZIP_UPLOAD_MAX_BYTES=6442450944
# NEXT_PUBLIC_LARGE_UPLOAD_API_URL=https://api.class-models.be
# COMBELL_UPLOAD_TIMEOUT_MS=21600000
# API_UPLOAD_TIMEOUT_MS=21600000
# ZIP_UPLOAD_CHUNK_BYTES=8388608
# Modellenportaal modeshow-downloads (map fotomodeshow-klein in mediatheek)
# MODEL_MODESHOW_FILM_FROM=2026-05-21T00:00:00+02:00
# MODEL_MODESHOW_PHOTOS_FOLDER=uploads,fotomodeshow-klein
# MODEL_MODESHOW_ZIP_NAME=Modeshow ok JPG klein .zip
# MODEL_MODESHOW_FILM_NAME=modeshow-film.mp4
