# Persistent media (Combell Node)

Bestanden in `uploads/` worden op de server gemapt naar `/app/shared/uploads` (`MEDIA_ROOT`).

Niet via de hosting file manager (`www/`, `data/`) — alleen via deploy van dit project.
