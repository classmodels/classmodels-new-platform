import { Cormorant_Garamond } from 'next/font/google';

/** Galerij-muurtekst: elegant serif, los van de standaard Georgia stack. */
export const wallLabelFont = Cormorant_Garamond({
  subsets: ['latin'],
  weight: ['600'],
  display: 'swap',
});
