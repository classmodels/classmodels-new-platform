import { GuestPortalLayout } from '@/components/guest-portal/GuestPortalLayout';

/** Publiek gastenportaal (geen login vereist). */
export default function GuestPortalPage() {
  return <GuestPortalLayout />;
}
