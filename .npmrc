# Combell / CI: `npm ci` met NODE_ENV=production installeert anders geen devDependencies
# → geen `tsc`, `nest`, `prisma`, … tijdens `npm run build`.
include=dev
# Monorepo (next 15 + react 19 + prisma in root): anders faalt `npm ci` vóór `npm run build`.
legacy-peer-deps=true
