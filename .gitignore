node_modules
# Nest-output; niet globaal "dist" — packages/shared/dist wordt wél gecommit (Combell).
apps/api/dist
.next
*.tsbuildinfo
.turbo
.env
.env.local
# Alleen lokaal: productie-DB voor CLI-scripts (nooit committen)
apps/api/.env.production
*.log
coverage
.DS_Store
apps/api/uploads
apps/api/data
apps/api/.deploy-media-bundle
.tmp-sparse-media
backups/
_tmp/
apps/web/.vercel
# Grote leveranciers-zips niet per ongeluk committen (map wél gebruiken voor import)
_import/**/*.zip
